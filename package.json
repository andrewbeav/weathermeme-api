{
  "name": "weathermeme_api",
  "version": "1.0.0",
  "description": "API that serves images based on the weather at a particular latitude and longitude",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Bevelhymer",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "python-shell": "^0.4.0"
  }
}
